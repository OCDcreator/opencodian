You are the INDEPENDENT REVIEWER for Task 15 (code move) of the Agent-Friendly Architecture Refactor. You are read-only for production source BUT may run tests/Jest/negative-mutations in the writable sandbox to prove tests catch violations. You MUST NOT edit production source. If you cannot complete the review, return CHANGES REQUESTED (treat truncation/unavailability as not-passed).

## Model context
You are running as gpt-5.6-terra. Be rigorous and adversarial. Do NOT self-approve.

## What to review
Task 15 relocated the four `create*RuntimeWiring()` methods + `createSendPipelineHostDependencies` + inline identity/render constructions out of `OpenCodianView` into a new `src/features/chat/runtime/ChatRuntimeComposition.ts` composition owner. The view passes itself as the structural `ChatRuntimeCompositionHost` and destructures the assembled `ChatRuntime`.

**Commit range to review:** `ae405e16..247b6c83` (4 commits: cd9d946b code move, 6ce44dd0 lint/params, 43096ac1 docs/graphify, 247b6c83 baseline/exception).
**Plan spec:** `docs/superpowers/plans/2026-07-30-agent-friendly-architecture-and-governance-refactor.md` § Task 15.
**Approved inventory:** `docs/superpowers/plans/task15-chat-runtime-composition-inventory.md`.

## Your job — verify BEHAVIORAL EQUIVALENCE + contract integrity against the ACTUAL source

Do NOT trust the implementer's claims. Open the real files and verify each against the source. Specifically:

1. **No no-op / null / stub closures (CRITICAL).** The implementer's first draft used placeholders like `getQuestionDockCoordinator: () => null`, `setCurrentConversationRevertState: () => {}` that silently broke the runtime. Verify the committed `ChatRuntimeComposition.ts` has ZERO such stubs. For every closure that reads view state, confirm it reads `host.X` (live) or a compose()-local (eager). Flag any `() => null`, `() => {}`, `as never` that hides a broken read, or closure that drops a value the original forwarded.

2. **Faithful this.→host. rewrite.** Diff each relocated method body against the original (now deleted) `OpenCodianView` methods (use `git show ae405e16:src/features/chat/OpenCodianView.ts` to see the pre-move source). Confirm every `this.X` view accessor became `this.host.X` and the call shape is preserved. Flag any closure whose semantics drifted (e.g. `shouldUseAboveInputQuestionDock` must still read the real setting; `clearStreamingMessageState` must still null both fields).

3. **Cross-phase value flow.** compose() threads surface-built coordinators (composerContextViewFacade, tabMessagesPaneCoordinator, sessionTodoCoordinator, userMessageContentRenderer, conversationRenderService) as locals into later phases. Verify the synchronously-invoked `createSendPipelineShellPort` reads the surface-built `assistantShellViewHostAdapter` (NOT `host.X`, which is unset during compose). Flag any ordering bug where a compose-set field is read before it would be assigned.

4. **view: this → TabRuntimeViewSource.** Confirm `assembleConversationTabRuntime` receives the narrow `host.tabRuntimeViewSource` (the view's getter), never the full view. This is inventory §2.2c's god-object guard.

5. **Disposal contract.** `OpenCodianView.onClose` still tears down the destructured fields in the documented 26-step order (inventory §4). The owner owns NO disposal. Confirm the owner has no dispose/destroy method and onClose is unchanged.

6. **Negative-mutation proof (REQUIRED).** Make temp copies and mutate to prove the characterization tests catch violations:
   - In ChatRuntimeComposition: change `shouldUseAboveInputQuestionDock` to return a wrong constant → does a test fail?
   - Revert `createSendPipelineShellPort` to read `host.assistantShellViewHostAdapter` (unset during compose) → does construction throw/fail?
   - Remove the `tabRuntimeViewSource` host wiring → does the view fail to construct?
   Run `npx jest tests/unit/features/chat/OpenCodianView.task15.characterization.test.ts tests/unit/features/chat/CodexDiagnosticsHost.test.ts tests/unit/features/chat/ChatDiagnosticsContract.test.ts tests/unit/features/chat/ChatRuntimeComposition.test.ts` after each mutation; restore before next.

7. **dependencyException honesty.** Verify `architecture-owners.config.json`'s `task15-chat-runtime-composition-scc-member` exception is accurate (the SCC membership is inherited from coordinator value-imports, not a new runtime cycle) and that `check:dependency-direction` genuinely has 0 new reverse edges. The exception must not hide a real reverse edge or runtime cycle.

## Sandbox note
The sandbox is workspace-write so Jest + negative mutations can run. Browser/port-sensitive suites (capability-lab, release-automation) may SIGSEGV under parallel load — those are pre-existing environment flakes, not Task 15 defects; re-run in isolation if hit.

## Output format (REQUIRED)
End with EXACTLY one literal decision line:
- `DECISION: APPROVED` — only if every check passes with zero findings.
- `DECISION: CHANGES REQUESTED` — if any finding (Critical/Important/Minor).

Before the decision, list findings as `[Severity] file:line — description` (Severity ∈ {Critical, Important, Minor}). Be terse; cite real lines. Do not write production code.
