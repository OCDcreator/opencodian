# ClaudeCodeAdapter

> **源码**: `src/core/agents/backend/ClaudeCodeAdapter.ts`
> **状态**: [REVIEW]

## 概述

`ClaudeCodeAdapter.ts` 是 Claude Code Agent SDK backend adapter。它实现 `AgentService`、`AgentChatCapability`、`AgentSessionCapability` 和 `AgentForkCapability`，通过注入式或 lazy-loaded SDK facade 调用 `query()`，并复用 Claude options builder、stream normalizer 和 permission bridge。

生产 runtime 通过 `ClaudeCodeSdkLoader` lazy-load 官方 SDK facade，避免插件启动时因为 SDK 包、bundled binary 或本机认证状态阻塞 OpenCodian 启动。生产构建会把 SDK 主包打进 `main.js`，并把当前平台 Claude Code binary 放入 `dist/node_modules/@anthropic-ai/claude-agent-sdk-<platform>/`。`claude-code` 已进入 `IMPLEMENTED_AGENT_BACKENDS`，但默认设置仍只启用 OpenCode；用户需要在 backend 设置中显式启用 Claude Code 并完成 Claude Code 认证。

## 职责

- 声明 Claude Code backend kind、显示名、状态和 Phase 1 capability 集合：`Chat`、`Sessions`、`Fork`、`Models`、`Thinking`、`FileOps`、`Shell`、`Todos`、`Permissions`。其中 `Fork` 支持 `forkSession()`，但不声明 `Branching`（revert/unrevert/diff 仍保持 OpenCode-only）。`Todos` capability 已于 2026-06-07 productized：Claude 使用 `TaskCreate`/`TaskUpdate`/`TaskList`/`TaskGet`/`TaskOutput`/`TaskStop`（NOT `TodoWrite`）；`SessionTodoCoordinator` 从 `TaskCreate`/`TaskUpdate` 工具流量按 incremental CRUD model 推导 task state，并复用现有 `SessionTodoDock`，同时在 Claude session reset/activation 时 replay stored `contentBlocks` 以恢复 persisted Task* state。`Permissions` capability 表示 Claude Code 支持 settings + chat permission-mode live switch（`default` / `acceptEdits` / `bypassPermissions` / `plan`）并通过 `setPermissionMode()` fan-out 到活跃 SDK query；runtime proof 仍是 readback，因为 SDK 没有可观察的 mode-change stream marker。`Thinking` capability 表示 Claude Code 支持 thinking blocks 的流式渲染；`AgentCapability.Thinking` 控制 effort selector 的显示，而 thinking blocks 的实际渲染由通用 `StreamController` + `ThinkingBlockRenderer` 处理，不依赖此 capability flag。**2026-06-07 Round 5 审计**：thinking 完整管道已代码验证——backend adapter (`ClaudeCodeOptionsBuilder.mapThinkingForSdk`)、stream normalizer (`ClaudeCodeStreamNormalizer`)、chat streaming (`StreamController`)、persistence (`ConversationIdentityRuntime`) 和 rehydration (`AssistantShellViewHostAdapter`) 均覆盖单元测试。新增 `ClaudeCodeThinkingPipeline.test.ts` (5 cases) 验证 SDK message → normalized chunk → streaming content block → persisted block 的完整流水线。但 runtime proof 仍是 `readback`：无 BUILD_ID 锚定的 live proof 证明 thinking blocks 在普通 Claude Code 聊天 UI 中实际可见渲染。
- 维护本地 session handle，用于后续 OpenCodian conversation 到 Claude SDK 会话的映射基础
- 捕获 Claude SDK stream 中的真实 `session_id`，把本地 handle alias 到 SDK session id，并在后续发送中通过 `options.resume` 恢复同一 Claude session
- 在 `sendMessage()` 中构造 SDK `query({ prompt, options })` 输入
- 对每个 Claude session 维护一个持久 streaming SDK `Query`：首次发送启动 `query({ prompt: AsyncIterable<SDKUserMessage>, options })`，后续发送把 user prompt 推入同一个 input channel；`sendMessage()` 仍按 OpenCodian 现有 contract 返回 per-turn `StreamChunk` async generator，并在 SDK `result` 消息边界结束本轮输出
- 将 SDK message/event 通过 `ClaudeCodeStreamNormalizer` 转换为 `StreamChunk`
- 将 `ClaudeCodePermissionBridge.canUseTool` 注入 SDK options
- 首次 SDK query 前会加载动态 MCP 配置缓存，即使调用方没有显式 `start()` adapter，也会把项目 `.opencode` 转换后的 Claude `mcpServers` 透传到 SDK options，用于 MCP stdio/runtime 配置接线验证
- 暴露 `getMcpServerCount()` / `getMcpServerNames()` 供 Capability Lab 与 Claude Code Tools tab 做只读 MCP runtime 检测；这些方法统计/列出静态 `options.mcpServers` 或动态加载缓存中的 MCP server，未加载时返回 0 / 空数组，名称按字母序输出。不提供 MCP authoring 能力，也不写 `.claude/mcp.json`
- 暴露 `getMcpServerRuntimeStatuses()` 作为 SDK `Query.mcpServerStatus()` 的只读 runtime status readback seam；优先复用活跃 query，临时 query 只用于没有活跃 runtime 时的状态读取，并在 `finally` 关闭。返回值只保留 server name、status、scope、serverInfo、tool names/count 和错误摘要，不输出完整 config、headers、env 或 secret。Claude Code Tools 标签用它显示 connected/failed/tool names，只作为 supporting evidence，不提供 MCP authoring，也不把 MCP runtime visibility 升格成 `.claude/mcp.json` authoring
- 暴露 `getRuntimeCatalog()` 作为 SDK `Query.supportedCommands()` / `Query.supportedAgents()` 的只读 runtime catalog readback seam：优先复用活跃 query；若活跃 query 存在但缺少 catalog 方法，直接返回 `null`，避免用临时 query 伪装 live readback；只有没有活跃 query 时才创建临时 query，并在 `finally` 关闭空 prompt input、abort controller 与 SDK query。返回值只保留 command `name` / `description` / `argumentHint` / `aliases` 与 agent `name` / `description` / `model`，会丢弃无 name 条目并排序。该 seam 不执行 slash command、不创建或编辑 agent、不写 `.claude/**`，只能作为 runtime catalog visibility/supporting evidence
- 暴露 `getPluginCount()` 供 Capability Lab 与 Claude Code settings runtime ecosystem 摘要做只读 plugin 检测；该方法返回 `options.plugins?.length ?? 0`，未配置时返回 0，不提供 plugin authoring 能力
- 暴露 `getSkillCount()` 供 Capability Lab 与 Claude Code settings runtime ecosystem 摘要做只读 skill 检测；该方法返回 `options.skills` 的计数（数组长度），`skills` 为 `'all'` 时返回 `-1` 表示"全量启用"，未配置时返回 0，不提供 skill authoring 能力
- 暴露 `getSkillsList()` 供 Capability Lab 与 Claude Code settings runtime ecosystem 摘要做只读 skill 名称列表诊断；该方法返回 `options.skills` 的防御性拷贝（`string[]`），`skills` 为 `'all'` 时返回 `'all'` sentinel，未配置时返回空数组，不提供 skill authoring 能力
- 暴露 `getProjectClaudeSkills()` 供 Claude Code settings 与 chat slash catalog 的项目技能发现表面读取 vault `.claude/skills/<name>/SKILL.md`；它委托 `discoverClaudeProjectSkills(this.options.vaultPath)` 做纯文件系统扫描，返回 `ClaudeProjectSkillInfo[]`，不依赖 SDK runtime query、不执行 skill、不写 `.claude/**`，也不提供 skill authoring 能力
- 暴露 `getPluginsList()` 供 Capability Lab 与 Claude Code settings runtime ecosystem 摘要做只读 plugin 名称列表诊断；该方法返回 `options.plugins` 的字符串化拷贝（处理 `unknown[]` 类型：字符串直接返回，对象 JSON 序列化），未配置时返回空数组，不提供 plugin authoring 能力
- 暴露 `getAgentDefinitionCount()` 供 Capability Lab 与 Claude Code settings runtime ecosystem 摘要做只读 agent definition 检测；该方法统计 `options.agent`（单个 selector，存在且非空时计 1）与 `options.agents`（map 的键数量）之和，未配置时返回 0，不提供 agent authoring 能力
- 暴露 `getAgentDefinitionsList()` 供 Capability Lab 与 Claude Code settings runtime ecosystem 摘要做只读 agent definition 名称列表诊断；该方法返回 `options.agent`（若存在且非空）后接 `options.agents` 的键列表，未配置时返回空数组，不提供 agent authoring 能力
- 对活跃持久 `Query` 暴露后端 live control：`setModel()`、`setPermissionMode()` 和 `reloadMcpServers()` 会分别委托 SDK `Query.setModel()`、`Query.setPermissionMode()` 和 `Query.setMcpServers()`；`setModel()` SDK source 已验证（发送 `{subtype:"set_model",model}` 控制请求），但模型实际切换至后续 API 调用未被 live-runtime 验证；没有活跃 query 时保持无害 no-op，MCP reload 会先刷新 adapter 缓存
- 暴露 `getRuntimeSettings()` 作为只读 runtime settings snapshot seam：优先复用活跃 SDK `Query.getSettings()`；若活跃 query 存在但 SDK 缺少该方法，直接返回 `null`，避免用另一个 runtime 的临时快照伪装为 live readback；只有没有活跃 query 时才创建临时 query 做 readback。临时 query 只发 `get_settings` 控制请求，`finally` 会关闭空 prompt input、abort controller 与 SDK query；SDK path 缺失或抛错时返回 `null`。Capability Lab 用它与 `inspectLastDiagnosticSdkOptions()` 交叉检查 live settings snapshot；这只是可观察的设置快照，不是 settings authoring，也不会把 File Checkpoint / Rewind、Fallback Model 或 MCP authoring 升格为 pass
- 暴露 `getContextUsage()` 作为只读 SDK context usage readback seam：优先复用活跃 SDK `Query.getContextUsage()`；若活跃 query 存在但 SDK 缺少该方法，直接返回 `null`，避免用临时 query 伪装 live readback；只有没有活跃 query 时才创建临时 query 做 readback。临时 query 会在 `finally` 关闭空 prompt input、abort controller 与 SDK query；SDK 方法缺失、不可用或抛错时返回 `null`。该 seam 仅提供运行时读数，不改变 capability matrix，也不提供 context authoring 或预算控制能力
- 暴露 `getAccountInfo()` 作为只读 SDK authenticated account readback seam：优先复用活跃 SDK `Query.accountInfo()`；若活跃 query 存在但 SDK 缺少该方法，直接返回 `null`，避免用临时 query 伪装 live readback；只有没有活跃 query 时才创建临时 query 做 readback。临时 query 会在 `finally` 关闭空 prompt input、abort controller 与 SDK query；SDK 方法缺失、不可用或抛错时返回 `null`。该 seam 只读取 SDK 报告的账号/认证摘要，不执行登录认证、不保存 settings、不改变 backend enablement，也不改变 capability matrix
- 暴露 `readRuntimeFile()` 作为只读 SDK `Query.readFile()` runtime file readback seam：优先复用活跃 SDK query；若活跃 query 存在但缺少 `readFile()`，直接返回 `null`，避免用临时 query 伪装 live readback；只有没有活跃 query 时才创建临时 query 做 readback，并在 `finally` 关闭空 prompt input、abort controller 与 SDK query。调用方传入路径和选项；当前 Settings UI 默认请求 `maxBytes: 4096` 且 `encoding: 'utf-8'`，adapter/SDK 也可接受 `base64`。返回值可能包含 `absPath`、`contents`、`truncated`。该 seam 仅证明 SDK runtime 可读回文件内容作为 supporting evidence，不 bypass permissions、不写文件、不写 `.claude/**`，不证明 checkpoint snapshot / rewind / restore，不改变 Fallback Model 或 capability matrix 分类
- 对 restart-sensitive Claude SDK options 暴露 `restartPersistentQueries(reason)`：只关闭活跃持久 query，不删除本地 session handle。下一次发送会重新创建 SDK query，并在已经捕获真实 SDK `session_id` 时带上 `options.resume`，用于让 settingSources、additionalDirectories、env、allowed/disallowed tools、limits 等只能在 query 启动时读取的设置生效
- 从真实 SDK `Query.supportedModels()` 读取模型目录；本地 facade 兼容官方 `ModelInfo.value/displayName` 与旧 fixture `id/name` 形状，避免把 `supportedModels()` 误挂到顶层 SDK module。模型目录查询的生命周期现在显式区分 owner：临时 model-catalog query 会在 `supportedModels()` `finally` 中关闭；runtime-reuse 路径复用活跃会话 query 仅借用 `supportedModels` 能力、不会关闭 live runtime query（避免误杀活跃会话与 `this.cleanup is not a function` 绑定错误）
- 接收 composer per-send `options.model` 与 `options.variant`，在 SDK query options 中映射为 Claude Code `model` 与 `effort`；若同一持久 query 仍活跃且仅 model 变化，model 会通过 `Query.setModel()` 尝试 live 更新；若 effort 变化，则关闭旧 query 并用已捕获的 SDK session id 重新启动 resumed query，确保下一轮发送应用新的 effort
- 将 SDK `onElicitation` callback 注入 options，生产 host 会把 elicitation 转成 OpenCodian question flow 的统一交互入口
- 将 runtime-only `hooks`、`sessionStore` / `sessionStoreFlush`、`outputFormat`、`plugins`、`skills`、`agent` 和 `agents` 注入 options builder；这些字段只允许由 backend runtime/诊断 owner 传入，不会保存到用户设置，也不会让 settings UI 宣称 hooks/skills/agent authoring 已完成
- `buildSdkOptions()` 现在支持 send-time `outputFormat` 覆盖：如果 `sendOptions.outputFormat` 存在且为对象，则优先使用 send-time 值，否则回退到 adapter 级别的 `this.options.outputFormat`。这使普通聊天路径的 `/json` 一次性触发能正确覆盖诊断/实验性的默认 schema
- 将自定义 `abortController` 和 `spawnClaudeCodeProcess` 注入 SDK options，绕开 Obsidian/Electron renderer 对 `child_process.spawn({ signal })` 的 `AbortSignal` 兼容问题
- 支持 `cancelStream()`、`stop()`、`dispose()` 的本地取消和资源清理
- 将 SDK stream 异常转换为 backend-labelled error chunk，避免发送管线无响应
- 在首次 `sendMessage()` 时 lazy-load 官方 SDK；单测仍可注入 fake facade，启动路径不会直接 import SDK
- 对已经持久化到 OpenCodian conversation metadata 的 Claude SDK session id 进行轻量恢复，避免 Obsidian reload 后同一个 `backendSessionId` 因 adapter 内存 Map 清空而在发送前失败；恢复后的第一次 `query()` 会先通过 SDK session catalog 验证该 id 存在且可比对身份匹配，然后才传入 `options.resume`
- 普通 chat resume 身份校验采用 fail-close：SDK `getSessionInfo(sessionId, { dir: vaultPath })` 不可用、查无结果、返回对象没有 `sessionId` / `id` 可比对身份、或返回了不同 id，都会在创建 resumed `query()` 前返回 `Claude Code resume validation failed` error chunk；已经活跃的持久 `Query` 后续发送不会重复 catalog lookup
- 已恢复的普通 chat 如果 SDK stream 后续返回了不同的 session id（无论来自 `message_metadata` chunk、usage chunk，还是非 hook 类型的 `backend_event` chunk，或原始 SDK `session_id` / `sessionId` 字段），adapter 会关闭该 runtime 并把本轮输出转换为 resume validation failure，避免把 conversation 静默重绑到另一个 Claude session。**hook 类型的 `backend_event` 例外**：`hook_started` / `hook_progress` / `hook_response` 系统事件携带 hook runtime 自身的内部 session id，该 id 不是 conversation 的会话标识，`resolveDiagnosticSessionId()` 会跳过 hook chunk 及其原始消息的 `session_id` 字段，避免在 resumed session 的首次 follow-up send 中触发 false-positive resume validation 错误
- 普通 resume 与诊断 resume-at 在代码层面显式分离：`buildSdkOptions()` 只使用 session 自己捕获的 `sdkSessionId` 做 ordinary resume，不接受外部注入的任意 resume-at id；`buildDiagnosticSdkOptions()` 专供 `runDiagnosticPrompt()` 使用，可接受任意 `resumeSessionId` 做诊断探针，但前提是调用方必须显式设置 `_diagnosticResumeAt: true`。`runDiagnosticPrompt()` 会在入口校验该标志：如果提供了 `resumeSessionId` 但没有设置 `_diagnosticResumeAt: true`，则直接抛错，防止 resume-at 被意外用于稳定聊天路径。返回前还会显式检查诊断结果是否与普通 session state 冲突，确保诊断 resume-at 绝不修改或重绑稳定聊天会话的 `sdkSessionId`
- 通过 SDK facade 暴露 `listSessions()`、`getSession()`、`renameSession()` 与 `forkSession()` 的基础委托；`forkSession()` 只接受已绑定真实 SDK session id 的会话，若仅有本地 `claude-code-*` handle（尚未捕获 SDK session id）会在适配器层提前拒绝，避免把临时 handle 误传给 SDK；fork 成功后以 SDK session id 建立新的本地 session state；`getSession()` 现在接受 `sessionStore` 选项并转发到 SDK，与 `listSessions()` / `getSessionMessages()` 等方法的 sessionStore 透传行为保持一致，允许 Capability Lab session detail probe 从 diagnostic store 读取会话数据
- 通过诊断级方法委托官方 SDK JSONL history/subagent transcript/sessionStore import 入口：`getSessionMessages()`、`listSubagents()`、`getSubagentMessages()` 和 `importSessionToStore()`；这些方法复用已捕获的 SDK session id 和 vault-scoped `dir`，当 SDK 不支持对应 API 或本地 session 已删除时显式报错，避免把缺能力或无效 session 误判为空历史。`importSessionToStore` 的 SDK-unavailable 路径、SDK 错误传播路径、以及 `listSubagents`/`getSubagentMessages` 的 stale-session 路径现在都有单元测试覆盖
- `listSessions()` 现在允许 runtime-only `sessionStore` / pagination 选项，Capability Lab 可以在本地 JSONL 与插件内存里的 diagnostic store 之间切换 session 列表来源
- 提供 `runDiagnosticPrompt()`：用 runtime-only `hooks`、`sessionStore`、`outputFormat`、`includeHookEvents`、`forwardSubagentText`、`agentProgressSummaries`、`persistSession`、`resumeSessionId`、`agent`、`agents` 覆盖启动一次隔离的诊断 query，返回原始 SDK 消息与经 `ClaudeCodeStreamNormalizer` 归一化后的 `StreamChunk[]`，专供 Capability Lab 做 structured output / hook / session-store / subagent-stream / resume / agent-definition runtime proof，不进入普通 chat path。该诊断 query 也会沿用 adapter options 中的 `permissionBridge.canUseTool`、`onElicitation` 和 `mcpServers` / 动态 MCP cache；这些 seam 已有 focused test 证明会进入 diagnostic SDK options，但仍只是诊断 proof，不把 Claude permission、question 或 MCP authoring 提升为稳定产品面。`buildDiagnosticSdkOptions()` 会将 `request.agent` / `request.agents` 透传到 options builder，若 request 未提供则回退到 `this.options.agent` / `this.options.agents`，使 Agent Definitions Readback Proof 和 Agent Definition Proof 都能验证 options 构建正确性
- 2026-05-28: 新增 `_diagnosticBypassPermissions` 标志：当设为 `true` 时，`buildDiagnosticSdkOptions()` 会将 `permissionMode` 覆盖为 `bypassPermissions`（设置 `allowDangerouslySkipPermissions: true`）并跳过 `canUseTool` 和 `onElicitation` 接线，使诊断探针在没有聊天流式 UI / 权限卡片宿主可用时仍能执行工具。此标志的作用域边界：证明 env/tool/budget 传播到 Claude/Bash 子进程，**不**证明权限审批 UX。权限审批能力仍由普通聊天 + live harness 路径独立证明
- 2026-05-29: 新增 `_diagnosticMaxTurns` 标志：当设为非 `undefined`/`null` 值时，`buildDiagnosticSdkOptions()` 会将 `diagnosticSettings.maxTurns` 覆盖为该值，使 Capability Lab 的 "Run Max Turns Proof" 探针可以在不修改用户实际设置的情况下强制使用低 `maxTurns`（如 `1`）来测试 SDK turn-limit 执行。探针通过 `runDiagnosticPrompt()` 发送多工具调用 prompt，扫描 `rawMessages` 中的 `result` 消息的 `subtype: 'error_max_turns'` 来验证 SDK 是否正确执行了 turn 限制
- 2026-05-30: 新增 `_diagnosticCanUseTool` 逃逸口：当提供且 `_diagnosticBypassPermissions` 不为 `true` 时，此回调替代 `permissionBridge.canUseTool` 进入诊断 SDK options。用于 Allowed Tools proof 的 Phase B（非绕过模式 + 合成审批回调），记录 SDK 请求审批的工具名。作用域边界：诊断专用逃逸口，不改变正常聊天路径的 bridge/host 架构
- 2026-05-30: 新增 `_diagnosticForcePermissionMode` 逃逸口：当设为 `'default'`/`'acceptEdits'`/`'plan'` 且 `_diagnosticBypassPermissions` 不为 `true` 时，`buildDiagnosticSdkOptions()` 会将 `diagnosticSettings.permissionMode` 覆盖为该值，即使 adapter settings 中为 `'bypassPermissions'`。用于 Allowed Tools proof Phase B 强制非绕过模式。运行时证据（BUILD_ID `feature-phase0-capability.202605300708`）：即使强制 `permissionMode: 'default'`，SDK subprocess 仍不调用合成 `canUseTool`，直接执行工具——`canUseTool` 回调在 `query()` 模式下是死路径
- 2026-05-30: 新增 `_diagnosticToolRestriction` 逃逸口：当设为 `string[]` 时，覆盖 SDK options 中的 `tools` 字段（从默认 preset 改为严格工具白名单）。用于 Allowed Tools proof Phase C 测试 SDK `tools` 选项是否在 init 消息中限制工具目录。`tools` 是 SDK 的工具可用性限制器（从模型上下文移除工具），与 `allowedTools`（仅自动审批快捷方式）语义不同。诊断专用——不影响普通 runtime 路径。用户面向设置描述为 "pre-allow"（自动审批），不是 "restrict to"（限制可用性），因此不允许将此诊断逃逸口直接映射到用户设置而不改变面向行为。**2026-06-02 defensive-copy bug fix**: `options.tools` 现在用 `[...request._diagnosticToolRestriction]` 创建防御性副本，防止调用方在 `runDiagnosticPrompt()` 返回后修改原数组泄漏到 `inspectLastDiagnosticSdkOptions()` 快照。与 `buildClaudeCodeOptions()` 对 `plugins`、`skills`、`allowedTools`、`disallowedTools`、`restrictedBuiltinTools` 的防御性拷贝行为保持一致
- 对活跃、checkpoint-enabled 的 SDK `Query` 暴露后端级 `rewindFiles(sessionId, userMessageId, { dryRun? })` 委托；当前没有接入稳定聊天按钮，调用方必须先完成 dry-run/确认设计。适配器层 `rewindFiles` 已有完整单元测试覆盖（不可用/正常委托/dryRun选项/错误传播/失效session），但生产聊天路径仍显式 gated 为 OpenCode-only。`rewindFiles` 现在强制 `dryRun` 安全默认：不传 options 或传 `{}` 等价于 `{ dryRun: true }`；`userMessageId` 为空或纯空白时立即抛错；显式传 `{ dryRun: false }` 会触发 `warn` 级日志作为审计追踪
- 2026-05-28: `runCheckpointRewindProbe()` 两阶段 probe。Phase 1：`enableFileCheckpointing: true` + `persistSession: true` + `bypassPermissions`，Write tool 写入探测文件，流式捕获 `phase1UserMessageUuid`、`toolUseTypes`、`sdkFilesPersistedEventCount`。Phase 1 新增 `applyFlagSettings({ fileCheckpointingEnabled: true })` seam 探测：在首个 `assistant` 消息后调用 `query.applyFlagSettings()` 测试运行时设置注入是否能激活 snapshot 创建，返回 `applyFlagSettingsAttempted` 和 `applyFlagSettingsError`。Phase 2：`resume` 同一 session，对每个候选 ID 调用 `query.rewindFiles(candidateId, { dryRun: true })`。**2026-06-02 cleanup bug fix**: probe file cleanup 现在由 `runCheckpointRewindProbe()` 的外层 try/finally 保证，即使 Phase 2 `sdk.query()` 抛异常也能清理探测文件。内部逻辑提取为 `executeCheckpointRewindProbe()`、`streamCheckpointPhase1()`、`streamCheckpointPhase2Rewind()`、`executeDryRunCandidates()`、`executeActualRewind()`、`attemptApplyFlagSettings()` 和 `buildProbeEarlyReturn()`，消除了 max-lines-per-function (337→~90) 和 complexity (53→<20) 警告。返回类型使用 named types (`CheckpointRewindProbeResult` 等)。**2026-05-30 Source-Backed Blocker 加固**：`isInteractive:false` 在 `_T()` (sdk.mjs ~line 58) 中硬编码；零 `isInteractive=true` 存在于打包 SDK；snapshot 创建被 React `useState` setter 门控；`rewindFiles()` 发送 `sdk_rewind_files` 控制请求到子进程检查始终为空的 file history；`sdkFilesPersistedEventCount` 预期 0（非交互模式不发出 snapshot 事件）；`applyFlagSettings({ fileCheckpointingEnabled: true })` seam 已运行时验证为 dead-end（BUILD_ID feature-phase0-capability.202605311013: 调用成功但 sdkFilesPersistedEventCount 保持 0，所有候选仍 canRewind:false）。GitHub Issue #236 (2026-03-17, 无 maintainer 回复) 未修复。SDK 0.3.157–0.3.158 (regressed: `setMaxListeners(abortSignal)` crash in Obsidian/Electron; monkey-patch workaround exists but 0.3.158 produces identical canRewind:false results to 0.3.145)。所有 10 个候选均为 canRewind:false。**No executable path remains**: Anthropic 必须在非交互代码路径中添加 snapshot 创建逻辑
- 2026-06-01: `runSetModelLiveProbe(targetModel)` 两阶段诊断 probe。Phase 1：用 `bypassPermissions` + `persistSession: true` 发送短 prompt，从 result 消息提取 `modelUsage` 获得 Phase 1 模型列表。调用 `query.setModel(targetModel)` 发送 `{subtype:"set_model",model}` 控制请求到 CLI 子进程。Phase 2：用另一个短 prompt 查询同一个 query，提取 Phase 2 `modelUsage`。返回 `setModelAttempted`、`setModelError`、`setModelNotAvailable`、`phase1ModelKeys`、`phase2ModelKeys`。Capability Lab 用它做分层判断：Phase 2 modelUsage 包含 targetModel 且 Phase 1 不包含 → `pass`（live switching verified）；setModel 成功但模型未变 → `readback`（控制消息已发送但切换未确认）；setModel 不可用 → `boundary`；异常 → `fail`。该 probe 是诊断专用，不改变稳定聊天行为或设置，也不改变 File Checkpoint / Rewind、Fallback Model 或 Allowed Tools 的分类。`extractModelUsageFromRaw()` 是模块级辅助函数，从 raw message 数组中提取最后一条 `type:'result'` 消息的 `modelUsage` 字段
- 2026-06-02: `runWarmStartupProbe()` 诊断 probe。调用 SDK 顶层 `startup()` 获取 `WarmQuery` handle，通过 warm handle 发送最小诊断 prompt，收集原始消息，返回 `WarmStartupProbeResult`。**Options pipeline 路由**：`startup()` 通过 `buildDiagnosticSdkOptions()` 构建适配器拥有的 options（包含 `vaultPath`、`spawnClaudeCodeProcess`、MCP、`bypassPermissions` 等），而非空参数调用。分类规则：`readback`（startup 解析 + warm query 产出消息；warm-vs-cold 延迟收益是 SDK 内部声明，非独立测量）、`fail`（startup 或 warm query 抛异常）、`boundary`（SDK facade 不暴露 startup()）。`WarmQueryHandle` 接口定义在 `ClaudeCodeSdkLoader.ts`，描述 SDK `WarmQuery` 的 `query(prompt)` + `close()` 形状。该 probe 是诊断专用，不改变稳定聊天行为、设置或 runtime owner，也不添加 authoring UI 或写入 `.claude/**`
- 2026-06-03: 六个诊断 probe 已实现完整逻辑（不再是 stub）：`runStderrDiagnosticProbe()` 返回 `StderrDiagnosticProbeResult`；`runContinueProbe()` 返回 `ContinueProbeResult`；`runResumeSessionAtProbe()` 返回 `ResumeSessionAtProbeResult`；`runForkSessionProbe()` 返回 `ForkSessionProbeResult`；`runSessionTitleProbe(requestedTitle)` 返回 `SessionTitleProbeResult`；`runCustomSessionIdProbe(targetSessionId)` 返回 `CustomSessionIdProbeResult`。这些 probe 通过 `runDiagnosticPrompt()` 执行真实的 SDK 查询，使用 nonce recall 和 session id 比对进行诚实分类。Fork probe 的成功路径会显式绕过 `validateDiagnosticResumeResult` 的同 session id 校验（因为 fork 语义期望返回不同的 session id），而 continue / resumeSessionAt / ordinary diagnostic resume 仍保持严格的 same-session 校验。
- 2026-06-04: `runStderrDiagnosticProbe()` 硬化：`truncateStderrPreview()` 提取为显式 helper，sanitize-before-truncate 边界明确（`sanitizeDiagnosticReport` 先于 `slice(0, 239) + '…'`）。`StderrDiagnosticProbeResult` 新增 `isolatedDiagnosticOnly: boolean` 字段，显式声明该探针使用隔离诊断回调，不影响活跃会话。新增测试覆盖：stderr 捕获、无 stderr 观察、sanitize 场景、aggressive truncation（≤240 字符）、sanitize-before-truncate 回归（跨截断边界的 secret 被正确 redact）、以及 `_diagnosticStderrCallback` 到达 built SDK options 的 wiring 证明。**2026-06-06 Outcome B 审计**：SDK 唯一 stderr API 是 `Options.stderr?: (data: string) => void`。审计结论：回调 wiring 已证明（readback），但无查询能可靠触发 stderr 输出——这是基本限制，非暂时空白。Stderr 输出是非结构化 CLI 内部文本，非契约性、不可解析、跨版本不稳定。Debug File（pass/verified）已通过确定性文件系统副作用覆盖"捕获调试输出"用例。Adjacent seams 全部 rejected：(a) live stderr 订阅（无用户工作流受益），(b) error-provoking query（error 场景产生结构化 SDK events 非 stderr），(c) debug=true+stderr（这是 Debug 能力，非 Stderr Diagnostic），(d) 结构化 stderr 解析（SDK 提供原始字节，非解析事件）。Promotion path：需 SDK 暴露结构化 stderr events 或契约性输出保证——截至审计时均不存在。分类保持 `readback`。
- 2026-06-03: 新增 `runPromptSuggestionsReadbackProbe()` 诊断 probe，返回 `PromptSuggestionsReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `promptSuggestions` 设置是否被正确映射到 SDK options，再结合显式模型选择状态给出 readback 结果。分类规则：`readback`（settings→SDK 映射已验证；实际 prompt suggestion 发射取决于 SDK/CLI 版本、effective model 和运行时条件，插件层无法独立验证）、`fail`（builder 映射与设置不一致或 probe 抛异常）。`modelState` 为三态：`claude`、`non-claude`、`unknown`。只有当用户**显式**选择 non-Claude 模型时才返回 `blockerNote`；空/默认模型选择会报告为 `unknown`，避免把继承的 SDK 默认模型误判为 blocker。**2026-06-04 硬化**：新增 3 个 focused fail-path 测试（enabled-but-missing、disabled-but-present、thrown-error），与原有的 3 个 pass-path 测试共同覆盖 probe 全部分支。**2026-06-06 分类提升**：该 probe 本身仍为诊断专用 readback seam，但整体 Prompt Suggestions 能力已从 `readback` 晋升为 `pass`（live proof 2026-06-06, BUILD_ID feature-phase0-capability.202606060953：Test Vault 普通聊天端到端观测到 suggestion chip，quick interaction proof 全部通过）。矩阵行 `runtimeProof` 现为 `'pass'`，诊断 probe 仍输出 `readback` 作为 settings→SDK mapping 的辅助证据。
- 2026-06-03: 新增 `runSystemPromptReadbackProbe()` 诊断 probe，返回 `SystemPromptReadbackProbeResult`。
- 2026-06-04: 新增 `runSystemPromptLiveProbe()` 诊断 probe，返回 `SystemPromptLiveProbeResult`。该 probe 执行真实 SDK 查询：生成随机 nonce，通过 `_diagnosticSystemPrompt` 注入包含 nonce 的诊断专用系统提示词，发送用户 prompt "What is the secret codeword?"（不包含 nonce），验证模型响应是否包含 nonce。分类规则：`pass`（模型响应包含 nonce，证明同一条 preset-with-append SDK 路径确实影响了响应）、`fail`（nonce 未被回忆、probe 抛异常）。**诚实边界**：live proof 使用诊断专用 append，不直接证明“当前保存的具体字符串”已被 live 执行；它需要与 `runSystemPromptReadbackProbe()` 的 settings→SDK option mapping 结果组合起来，才构成最终的 `pass` 结论。nonce 从不出现在用户 prompt 中，排除简单 prompt echo；这是 fresh diagnostic query，活跃普通聊天会话不会被修改；仅在下一次查询或重启后生效。该 probe 将 System Prompt 的矩阵分类从 `readback` 晋升为 `pass`。
- 2026-06-03: 新增 `runPlanModeInstructionsReadbackProbe()` 诊断 probe，返回 `PlanModeInstructionsReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `planModeInstructions` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；planModeInstructions 为空/whitespace 时 SDK options 中不应存在 planModeInstructions 字段；planModeInstructions 非空时 SDK options 中应存在 planModeInstructions 且值等于 trimmed setting；builder 不检查 permissionMode，因此无论 permissionMode 是否为 plan，只要 setting 非空就会映射到 SDK options——probe 忠实记录此当前映射行为，不将其误判为 fail）、`fail`（builder 映射与设置不一致或 probe 抛异常）。输出明确说明这是诊断 readback only，actual plan-mode behavior（read-only preamble + ExitPlanMode protocol footer enforcement）是 SDK 的内部声明，插件层无法独立验证，且仅在下一次查询或重启后生效。该探针是诊断专用 readback seam，不改变 Plan Mode Instructions 的矩阵分类（仍为 `readback`），也不声称行为验证通过。
- 2026-06-06: 新增 `runPlanModeInstructionsLiveProbe()` live probe，返回 `PlanModeInstructionsLiveProbeResult`。该 probe 通过 `_diagnosticForcePermissionMode: 'plan'` 强制在计划权限模式下运行诊断查询，通过 `_diagnosticPlanModeInstructions` 覆盖字段注入含 nonce 的计划模式指令（不修改用户保存的设置），通过 `_diagnosticCanUseTool` 自动批准工具调用（防止权限弹窗）。模型被要求回复 nonce；如果 nonce 出现在响应中，证明 planModeInstructions 在计划模式下实际到达了模型上下文并影响了响应。分类：`pass`（nonce 被回忆——真实行为验证）、`fail`（nonce 未被回忆或 probe 抛异常）。新增 `_diagnosticPlanModeInstructions` 请求覆盖字段，在 `resolveDiagnosticSettings()` 中与 `_diagnosticSystemPrompt` 和 `_diagnosticDebugFile` 同模式处理。2026-06-06 Codex Test Vault 验证（BUILD_ID `feature-phase0-capability.202606061246`）确认 nonce `1upt25vk` 被成功回忆，matrix 已从 `readback` 晋升为 `pass`。
- 2026-06-03: 新增 `runSandboxReadbackProbe()` 诊断 probe，返回 `SandboxReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `sandbox` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；sandbox.enabled 为 false 时 SDK options 中不应存在 sandbox 字段；sandbox.enabled 为 true 时 SDK options 中应存在 sandbox 对象且 enabled 为 true；failIfUnavailable 和 autoAllowBashIfSandboxed 为 false 时必须保持省略而不是显式传 `false`；实际 OS-level sandbox 强制执行无法从插件层独立验证）、`fail`（任一子字段映射不一致或 probe 抛异常）。输出明确说明这是诊断 readback only，OS-level sandbox enforcement（bubblewrap/seccomp 等）是 SDK/CLI binary 的内部声明，插件层无法独立验证，且仅在下一次查询或重启后生效。**2026-06-06 审计**：完整追踪 SDK sandbox 路径——Options.sandbox → SDK `initialize()` 解构 → `X2()` 合并进 `--settings` JSON → CLI 子进程。SDK 默认行为：`enabled=true` 时 X2() 将 `failIfUnavailable` 默认为 `true`（sdk.d.ts line 1662 确认）。无任何可观测信号确认沙箱激活：无 init 事件、无工具元数据、无 stderr 模式、无 `CLAUDE_CODE_SANDBOXED` 环境变量（该变量仅存在于 assistant-worker 路径，非 `createQuery` 路径）。分类保持 `readback`：插件无法区分"沙箱活跃"、"沙箱静默降级"或"不受支持的平台"。推广路径：若 SDK 未来在 init 事件或工具结果元数据中添加沙箱状态字段，可重新审计。
- 2026-06-03: 新增 `runToolAliasesReadbackProbe()` 诊断 probe，返回 `ToolAliasesReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `toolAliases` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；toolAliases 为空对象时 SDK options 中不应存在 toolAliases 字段；toolAliases 非空时 SDK options 中应存在 toolAliases，条目值一致，且必须保持与 settings 不同的对象引用，防止修改泄漏；实际 alias resolution 行为无法从插件层独立验证）、`fail`（条目数量不匹配、键值不匹配、same-object-reference 泄漏或 probe 抛异常）。**2026-06-06 审计硬化**：SDK 源码（browser-sdk.js）确认 toolAliases 是单向初始化参数——`initialize()` 方法将其作为 `toolAliases: this.initConfig?.toolAliases` 转发给 CLI 子进程，无反馈事件或状态确认。别名解析发生在 CLI binary 的内部工具执行路径中；流式 `tool_use` 块仅暴露解析后的名称，无别名元数据。插件无法区分别名调用与直接规范调用，因此 alias resolution 行为无法从插件层独立验证。输出明确说明这是诊断 readback only，且仅在下一次查询或重启后生效；active sessions 不会 live update。该探针是诊断专用 readback seam，不改变 Tool Aliases 的矩阵分类（仍为 `readback`），也不声称行为验证通过。
- 2026-06-03: 新增 `runDebugFileReadbackProbe()` 诊断 probe，返回 `DebugFileReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `debugFile` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；debugFile 为空/whitespace 时 SDK options 中不应存在 debugFile 字段；debugFile 非空时 SDK options 中应存在 debugFile 且值等于 trimmed setting；实际 CLI debug file writing 行为无法从插件层独立验证）、`fail`（builder 映射与设置不一致或 probe 抛异常）。输出明确说明这是诊断 readback only，actual CLI file writing is not independently verified，且仅在下一次查询或重启后生效；设置 debug file path 会隐式启用 debug logging（即使 debug toggle 为 off）。**2026-06-06 分类提升**：新增 `runDebugFileLiveProbe()` live probe，返回 `DebugFileLiveProbeResult`。该 probe 创建临时目录，通过 `_diagnosticDebugFile` 请求覆盖字段将 debugFile 设为临时路径，运行真实诊断查询，然后检查 CLI 子进程是否在共享文件系统上创建了非空文件（`fs.existsSync` + `fs.statSync`）。分类：`pass`（文件存在且非空——真实可观测副作用）、`fail`（文件不存在或为空）。Debug File 整体能力从 `readback` 晋升为 `pass`。原有的 readback probe 保留为 supporting evidence。`debugFile` 通过 `--debug-file <path>` CLI 标志传递给子进程（非 `--settings` JSON），且隐式启用 debug mode（sdk.d.ts line 1773-1777 确认："Implicitly enables debug mode"）。
- 2026-06-03: 新增 `runStrictMcpConfigReadbackProbe()` 诊断 probe，返回 `StrictMcpConfigReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `strictMcpConfig` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；strictMcpConfig 为 false 时 SDK options 中不应存在 strictMcpConfig 字段；strictMcpConfig 为 true 时 SDK options 中应存在 strictMcpConfig 且值为 true；实际 MCP config validation 行为无法从插件层独立验证）、`fail`（false 但 option 存在、true 但 option 缺失/false、或 probe 抛异常）。**2026-06-06 审计硬化**：SDK 将 strictMcpConfig 作为 `--strict-mcp-config` CLI 标志传递给子进程（sdk.mjs: `if(v4)i.push("--strict-mcp-config")`）。实际验证行为位于编译后的 CLI binary 中，SDK wrapper 不执行验证。没有结构化信号（无 init event 字段、无 result subtype、无 stderr contract）确认严格验证是否已应用。插件侧 adapter（ClaudeCodeMcpConfigAdapter.ts）会静默丢弃结构性 malformed 条目（返回 null），因此许多 malformed 配置从未到达 CLI。malformed-config 探针将依赖 CLI 版本和运行时条件，不具有确定性。输出明确说明这是诊断 readback only，actual MCP config validation is not independently verified，且仅在下一次查询或重启后生效；active sessions 不会 live update；不写入 `.claude/mcp.json`，也不提供 MCP authoring。该探针是诊断专用 readback seam，不改变 Strict MCP Config 的矩阵分类（仍为 `readback`），也不声称行为验证通过。
- 2026-06-04: 新增 `runDebugReadbackProbe()` 诊断 probe，返回 `DebugReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `debug` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；debug 为 false 时 SDK options 中不应存在 debug 字段；debug 为 true 时 SDK options 中应存在 debug 且值为 true；actual CLI debug log emission 无法从插件层独立验证）、`fail`（builder 映射与设置不一致或 probe 抛异常）。输出明确说明这是诊断 readback only，actual CLI debug log emission is not independently verified，且仅在下一次查询或重启后生效；active sessions 不会 live update；debugFile 是 separate seam。该探针是诊断专用 readback seam，不改变 Debug 的矩阵分类（仍为 `readback`），也不声称行为验证通过。
- 2026-06-04: 新增 `runContext1mBetaReadbackProbe()` 诊断 probe，返回 `Context1mBetaReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `betas` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；enableContext1mBeta 为 false 时 SDK options 中不应存在 betas 字段；enableContext1mBeta 为 true 时 SDK options 中应存在 betas 且值为 `['context-1m-2025-08-07']`；实际 beta 可用性取决于所选模型和 Anthropic-side 行为，无法从插件层独立验证）、`fail`（false 但 option 存在、true 但 option 缺失/值不匹配/长度不对、或 probe 抛异常）。输出明确说明这是诊断 readback only，actual beta availability depends on selected model and Anthropic-side behavior，且仅在下一次查询或重启后生效；active sessions 不会 live update；不暴露通用 beta 管理功能，仅覆盖单一已记录 beta seam。该探针是诊断专用 readback seam，不改变 1M Context Beta 的矩阵分类（仍为 `readback`），也不声称行为验证通过。**2026-06-06 审计硬化**：完整 SDK 路径已追踪——`settings.enableContext1mBeta` → `buildClaudeCodeOptions` → SDK `Options.betas` → `ProcessTransport.initialize()` → CLI `--betas` 标志（sdk.mjs: `if(J&&J.length>0)i.push("--betas",J.join(","))`）。SDK init 消息（`type:'system', subtype:'init'`）包含 `betas?: string[]` 字段，CLI 子进程在此回显已确认的 betas，但插件当前不消费该字段。Readback 上限：插件验证 option wiring 到 CLI 子进程边界；无法验证模型侧 beta 接受、1M 上下文激活或 API 密钥资格。晋升路径：消费 init message betas 字段 → 更强 readback。
- 2026-06-04: 新增 `runJsRuntimeReadbackProbe()` 诊断 probe，返回 `JsRuntimeReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `executable` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；jsRuntime 为空字符串时 SDK options 中不应存在 executable 字段；jsRuntime 为 `node`/`bun`/`deno` 时 SDK options 中应存在 executable 且值与 setting 完全一致；实际运行时选择取决于 SDK/CLI 版本、系统 PATH 和运行时安装情况，无法从插件层独立验证）、`fail`（空 setting 但 option 存在、非空 setting 但 option 缺失、非空 setting 但值不匹配、或 probe 抛异常）。输出明确说明这是诊断 readback only，actual runtime selection depends on SDK/CLI version, system PATH, and whether the requested runtime is installed，且仅在下一次查询或重启后生效；active sessions 不会 live update；不暴露运行时参数管理功能（`executableArgs` / `extraArgs` 明确未实现）。该探针是诊断专用 readback seam，不改变 JS Runtime 的矩阵分类（仍为 `readback`），也不声称行为验证通过。
- 2026-06-04: 新增 `runLoadTimeoutReadbackProbe()` 诊断 probe，返回 `LoadTimeoutReadbackProbeResult`。该 probe 不执行真实 SDK 查询，但会调用 `buildDiagnosticSdkOptions()` 构建一次隔离的诊断 options，直接验证 `loadTimeoutMs` 设置是否被正确映射到 SDK options。分类规则：`readback`（settings→SDK 映射已验证；loadTimeoutMs 为 null 时 SDK options 中不应存在 loadTimeoutMs 字段；loadTimeoutMs 为正整数时 SDK options 中应存在 loadTimeoutMs 且值与 setting 完全一致；实际超时行为取决于 SDK/CLI 版本和运行时条件，无法从插件层独立验证）、`fail`（null setting 但 option 存在、非 null setting 但 option 缺失、非 null setting 但值不匹配、或 probe 抛异常）。输出明确说明这是诊断 readback only，actual timeout behavior depends on the SDK/CLI version and runtime conditions，且仅在下一次查询或重启后生效；active sessions 不会 live update。该探针是诊断专用 readback seam，不改变 Load Timeout 的矩阵分类（仍为 `readback`），也不声称行为验证通过。**2026-06-06 审计硬化**：SDK 仅在 `(options.resume || options.continue) && options.sessionStore` 为真时使用 loadTimeoutMs（sdk.mjs `yj$` 函数）。超时包装器为 `C4` 函数（Promise.race + setTimeout，offset 154014），超时时 reject 并传播到 `yj$.catch` → `transport.spawnAbort(error)` + `queryInstance.setError(error)`。没有 resume/continue + sessionStore 时超时代码永不执行；诊断路径不使用这些选项，无法触发超时。晋升路径：SDK 暴露通用查询超时或插件获得 sessionStore 访问。
- 通过 `createLogger('ClaudeCodeAdapter', { moduleKey: 'claudeCode', channel })` 写入摘要级诊断日志：`runtime` 覆盖 start/stop/dispose/status change、sendMessage、runtime create/reuse/close、SDK load/query creation、supportedModels、spawn command/exit/error；`sessions` 覆盖 create/delete/update/list/get/fork/rewind/restore；`mcp` 覆盖 MCP config load/reload。日志只记录 id、cwd、count、length、状态和错误摘要，不记录 prompt、tool input、secret 或完整 env
- `buildSdkOptions` 在构造完 SDK options 后记录 `buildSdkOptions tool config` 日志，包含 `allowedToolCount`、`disallowedToolCount`、`allowedTools` 和 `disallowedTools`，用于在运行时验证工具策略是否进入 SDK query 选项
- `buildSdkOptions` 同时记录 `buildSdkOptions limits config` 日志，包含 `maxTurns` 和 `maxBudgetUsd`（未设置时为 `null`），用于在运行时验证限制项是否进入 SDK query 选项
- `buildSdkOptions` 还记录 `buildSdkOptions fallback model` 日志，包含 `fallbackModel` 值（未设置时为 `null`），用于在运行时验证 fallback model 是否进入 SDK query 选项。2026-05-31 CLI-binary-backed blocker hardened：SDK source (sdk.mjs v0.3.145) 只负责 options destructure、same-model validation 和推送 `--fallback-model` CLI arg；ZERO switching logic in SDK。真正切换位于 compiled `claude` executable 的 API retry loop：`FallbackTriggeredError` 由 HTTP 529 / `overloaded_error` 且满足 Opus-family 或 `FALLBACK_FOR_ALL_PRIMARY_MODELS` 门控后触发，阈值为 3 consecutive 529s (`dY3=3`)，随后切到 fallbackModel、发 telemetry 并显示 "Switched to ..." notification。Same-model validation 确定性抛出（`fallbackModel === model`），但不证明切换行为。Invalid-primary test (BUILD_ID feature-phase0-capability.202605300441) 已失效；`modelUsage` 只是 passive detection seam。无法模拟真实 API overload。Blocker = switching behavior not locally provable。Next executable path: Anthropic 暴露 programmatic fallback trigger，或接受 readback 为诚实上限
- 2026-05-28: prompt hardening 尝试（commit `1c7a380a`）已被 revert，因为运行时 artifact 证明它未能稳定消除 structured output 路径中的额外 prose；该问题目前归为 SDK/model 边界
- 新增 `inspectLastDiagnosticSdkOptions()` 公共诊断方法：返回最后一次 `buildDiagnosticSdkOptions()` 构建的 `ClaudeCodeSdkOptionsShape` 的**深拷贝副本**（使用 `structuredClone` 或 JSON fallback），供 Capability Lab 的 Stable Settings Readback Proof 使用。返回副本而非活对象，确保诊断 UI 不会意外污染 adapter 内部状态。该方法只读取内部缓存的 `lastDiagnosticSdkOptions`，不触发新的 SDK 调用，也不暴露普通聊天路径的 options。它使稳定设置（allowedTools、disallowedTools、maxTurns、maxBudgetUsd、env、fallbackModel）的 runtime-readback proof 成为可能：探针运行诊断 prompt 后，可以验证实际传入 SDK 的 options 中是否包含这些字段，从而区分 "wiring-only" 和 "runtime-readback verified"。
- `ClaudeCodeDiagnosticPromptRequest` 接口现在接受可选 `fallbackModel?: string` 字段，允许 Capability Lab 诊断探针在运行诊断 prompt 时覆盖 fallback model；同时接受可选 `model?: string` 字段，允许诊断探针使用故意无效的主模型名称来触发 fallback 行为验证
- `buildDiagnosticSdkOptions()` 将 `request.fallbackModel` 和 `request.model` 透传到 options builder，用于诊断级的模型覆盖验证
- `runFallbackModelProof()` 现在使用故意无效的主模型（`opencodian-invalid-model-test-xyz123`）配合有效 fallback 模型运行诊断 prompt，通过 `inspectLastDiagnosticSdkOptions()` 验证选项回读（Layer 1），通过检查返回的 `message_metadata` chunk 中的 `modelId` 验证 fallback 行为（Layer 2）。分层判断：Layer 2 通过 → `pass`，Layer 1 通过但 Layer 2 未确认 → `readback`，Layer 1 也未通过 → `fail`。当前行为证据始终失败（无效主模型被 SDK 接受无错误，回显相同无效字符串而非触发 fallback），因此运行时标记为 `readback`（选项回读通过）
- 2026-05-28: `ClaudeCodeDiagnosticPromptRequest` 接口新增可选 `agent?: string` 和 `agents?: Record<string, AgentDefinition>` 字段，允许 Capability Lab 诊断探针在运行诊断 prompt 时传入 inline agent definitions。`buildDiagnosticSdkOptions()` 将 `request.agent` 和 `request.agents` 透传到 options builder，优先使用请求中的值，回退到 adapter options 中的值。这使得 Capability Lab 可以通过 "Run Agent Definition Proof" 按钮验证 SDK 是否接受并应用内联 agent definitions：探针构造一个带特定标记 prompt 的 proof agent，通过 `agent` 选择器激活，然后检查 assistant 回复是否包含该标记。分层验证：Layer 1 检查 SDK options readback（agent/agents 是否进入 options），Layer 2 检查 assistant behavior（模型是否按 agent definition 的 prompt 指示回复）。如果 SDK 拒绝内联 agent（如 "Unknown agent" 错误），探针会诚实标记为 `fail` 并记录该 blocker 发现

## 维护约束

- 不直接静态 import 官方 SDK；真实 SDK 只能通过 `ClaudeCodeSdkLoader` 动态加载，方便测试继续使用 fake facade，并避免 Jest/Obsidian 启动期 ESM 解析问题。
- adapter 的 `debug` / `info` 诊断日志必须继续受全局 `enableDebugLogging`、`claudeCode` debug module 和 Claude Code channel 设置控制；`warn` / `error` 可沿用 shared logger 默认行为。
- OpenCode 仍是默认 backend；Claude Code 必须通过设置显式启用，认证失败会以 Claude Code error chunk 暴露，而不是回落到 OpenCode。
- `spawnClaudeCodeProcess` 不要把 SDK 传入的 `signal` 继续传给 Node `spawn()` options；它需要手动监听 abort 并 kill 子进程，保持 Obsidian renderer 兼容。
- 本地 handle 与 SDK `session_id` 是两层身份：首次发送前只有本地 handle，SDK 输出 `session_id` 后才把 conversation `backendSessionId` 收敛为真实可 resume 的 Claude session id；如果 reload 后传入的 `backendSessionId` 已经是 Claude SDK session id，adapter 会直接恢复 `sdkSessionId` 并 resume。
- 持久 `Query` 关闭或 adapter reload 后，下一次发送会重新启动 SDK query，并在已捕获真实 SDK `session_id` 时继续通过 `resume` 恢复；OpenCodian 本地生成的 `claude-code-*` conversation handle 不会被误传给 SDK `resume`，而持久化的真实 SDK id 必须先通过 ordinary chat catalog validation。JSONL message/subagent/sessionStore 方法当前只是后端 foundation，完整 history browser/import UI、hook/agent/skill authoring UI 和 stable rewind UI 仍属于后续 full-capability phase。
- `runDiagnosticPrompt()` 属于 Capability Lab 的 runtime-only side channel：它可以选择 `persistSession: false` 做无痕诊断，也可以附带 `sessionStore` 做 mirror/import proof，或通过 `resumeSessionId` 验证 SDK `options.resume` 是否能恢复既有 Claude session；无论哪种都不能修改稳定 chat runtime owner、settings 持久化或 user-facing capability gating。`resumeSessionId` 只允许传入 SDK `getSessionInfo(resumeSessionId, { dir: vaultPath })` 能在 Claude session catalog 中确认存在的会话；SDK lookup 不可用、返回空，或返回对象显式携带了不匹配的 `sessionId` / `id` 时，都会在 `sdk.query()` 前抛出诊断恢复验证错误，避免把 placeholder / OpenCode / 本地 handle / 交叉命中的 Claude session 误传给 Claude SDK `resume`。如果 SDK lookup 成功但返回对象没有可比对的 id 字段，则继续按兼容路径放行。诊断 query 返回后还会二次校验实际 `result.sessionId`：带 `resumeSessionId` 的诊断必须返回同一个 SDK session id，否则视为恢复失败并抛错；Capability Lab resume 探针也会把这种错配标记为 runtime fail，而不是把新开的 fresh session 误判为 resume proof。
- 当 `runDiagnosticPrompt()` 附带 `sessionStore` 时，会显式关闭 `enableFileCheckpointing` runtime override；这是为了遵守官方 SDK 对该组合的限制，避免 sessionStore 诊断 probe 因 checkpoint blob 不镜像而直接失败。
- Claude Code 目前不注册 `Hooks` / `Subagents` UI capability，避免误打开 OpenCode-only 子代理或 hooks surface；SDK hook/subagent 事件先以 diagnostic `backend_event` 进入发送调试链路。
- crash recovery 当前只做错误 chunk；异常后的 prompt replay、冷启动 fallback 和多 view 并发同 session 仲裁属于后续 full-capability phase。
- 2026-06-06: prompt-suggestion sink registration 移入 `ensureReadyForQuery()`（每次 `sendMessage`/`createSession` 调用时触发），而非仅在 `start()` 中。这修复了真实产品路径中 adapter 从未被显式 `start()` 导致 sink 未注册、coordinator 无法 attach `onPostResultChunk` 的 live blocker。`registerPromptSuggestionSink` 现在是幂等的——同一 adapter 对象重复注册不会重发 callback。`start()` 保留其现有注册调用（幂等，无副作用）。`stop()`/`dispose()` 清理仍正确。
