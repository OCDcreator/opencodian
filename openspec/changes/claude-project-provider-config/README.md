# claude-project-provider-config

Claude Code backend: project-level third-party provider/model presets written to `.claude/settings.local.json`, read-only global config display, and resources tab split into Tools / MCP / Skills & Commands / Agents.
